# .htaccess
RewriteEngine On
RewriteBase /

# If the request is not for a file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Rewrite /register to register.php
RewriteRule ^register$ register.php [L,QSA]